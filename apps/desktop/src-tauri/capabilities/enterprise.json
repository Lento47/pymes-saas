{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "enterprise",
  "description": "Capacidades adicionales para la edición enterprise (offline).",
  "windows": ["main"],
  "permissions": [
    "shell:allow-execute",
    "shell:allow-spawn"
  ]
}
