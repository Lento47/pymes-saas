# Railway Docker — solo backend
# Excluir frontend del contexto de build. pnpm necesita ver el manifest
# del workspace web porque el workspace root incluye apps/*.

apps/web/*
!apps/web/package.json

apps/desktop/

.git/
node_modules/
